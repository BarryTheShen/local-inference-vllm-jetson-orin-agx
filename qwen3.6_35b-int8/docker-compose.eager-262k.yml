# Experimental overlay: mandatory 262K context without CUDA graph capture.
# Fun-ASR must remain stopped during this experiment.
#   docker compose -f docker-compose.yml -f docker-compose.eager-262k.yml up
services:
  vllm-35b-int8:
    restart: "no"
    command:
      - /models/Qwen3.6-35B-A3B-INT8-AutoRound
      - --served-model-name=qwen3.6-35b-a3b-int8
      - --host=0.0.0.0
      - --port=8000
      - --trust-remote-code
      - --tokenizer=/models/Qwen3.6-35B-A3B-tokenizer
      - --dtype=bfloat16
      - --max-model-len=262144
      - --enforce-eager
      - --gpu-memory-utilization=0.75
      - --max-num-seqs=1
      - --max-num-batched-tokens=2096                  # Mamba align minimum; 512 is rejected by vLLM
      - --enable-prefix-caching
      - --kv-cache-dtype=fp8
      - --attention-backend=FLASHINFER
      - --reasoning-parser=qwen3
      - --enable-auto-tool-choice
      - --tool-call-parser=qwen3_xml
